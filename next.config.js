/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  trailingSlash: true,
  output: 'export',
  basePath: '/Super',
  assetPrefix: '/Super',
}

module.exports = nextConfig